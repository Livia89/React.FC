import { RoutesApp } from 'routes';
import { GlobalStyles, Theme } from 'themes';

export const App = () => {
  return (
    <Theme>
      <GlobalStyles />
      <RoutesApp />
    </Theme>
  );
};

//export default App;
